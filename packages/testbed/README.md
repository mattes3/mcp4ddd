# Testbed for experimenting with DDD component generation

This package provides a safe environment to test the MCP server
and experiment with generated DDD components without affecting
the core packages.

Use the prompts in the prompts/ directory to test different scenarios.
