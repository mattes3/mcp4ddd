import { describe, it, expect } from 'bun:test';

describe('{{serviceName}}', () => { 
    it('{{serviceName}}', async () => { 
        // test the method {{serviceName}}({{formattedParameters}}): {{{asyncResultType}}}
    });
});
