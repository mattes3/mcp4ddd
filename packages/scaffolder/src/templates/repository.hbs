import { TechError } from '{{{basicErrorTypesFrom}}}';
import type { AsyncResult, Option } from 'ts-results-es';

// Using .js extensions for ESM compliance with TypeScript's nodenext moduleResolution
import type { {{aggregateName}}, {{aggregateName}}Data } from './{{aggregateName}}.js';

// @Repository
export interface {{repositoryName}} {
{{#each methods}}
    {{name}}(params: { {{{formattedParameters}}} }): {{{formattedResultType}}};

{{/each}}
};
