import { TechError } from '{{{basicErrorTypesFrom}}}';
import type { AsyncResult, Option } from 'ts-results-es';

import type { {{aggregateName}}, {{aggregateName}}Data } from './{{aggregateName}}.ts';

// @Repository
export interface {{repositoryName}} {
{{#each methods}}
    {{name}}(params: { {{{formattedParameters}}} }): {{{formattedResultType}}};

{{/each}}
};
