//TODO: Add imports for the types that are being used in the parameter lists of the methods inside the repository interface, below.

// @Repository
export interface {{repositoryName}} {
{{#methods}}
    {{name}}(params: { {{formattedParameters}} }): Promise{{{angle resultType}}};
{{/methods}}
};
