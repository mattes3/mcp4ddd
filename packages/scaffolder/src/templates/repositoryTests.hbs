import { describe, it } from 'node:test';
import { expect } from 'expect';

describe('{{repositoryName}}', () => { 
{{#each methods}}
    it('method {{name}}', async () => { 
        // test the method {{name}}({{{formattedParameters}}}): {{{formattedResultType}}}
    });

{{/each}}
});
