import { describe, it } from 'node:test';
import { expect } from 'expect';

describe('{{valueObjectName}}', () => { 
    it('should create an instance', () => { 
        // TODO: test 
    });

{{#methods}}
    it('{{name}}', () => { 
        // test the method {{name}}({{formattedParameters}}){{#if resultType}}: {{resultType}}{{/if}}
    });

{{/methods}}
});
