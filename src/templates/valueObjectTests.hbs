import { describe, it } from 'vitest';

describe('{{valueObjectName}}', () => { 
    it('should create an instance', () => { 
        // TODO: test 
    });

{{#methods}}
    it('{{name}}', () => { 
        // test the method {{name}}({{formattedParameters}}){{#resultType}}: {{resultType}}{{/resultType}}
    });

{{/methods}}
});
