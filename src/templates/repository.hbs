/**
 * @Repository
 *
 * Stores {{aggregateName}} objects and
 * finds them according to certain criteria.
 */
export interface {{repositoryName}} {
{{#methods}}
    {{name}}({{formattedParameters}}){{#if resultType}}: Promise{{{angle resultType}}}{{/if}};
{{/methods}}
};
