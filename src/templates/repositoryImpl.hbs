/**
 * Repository implementation.
 *
 * Stores {{aggregateName}} objects and
 * finds them according to certain criteria.
 */
export const {{repositoryName}}Impl: {{repositoryName}} = {
{{#methods}}
    async {{name}}({{formattedParameters}}): Promise{{{angle resultType}}} {
        // TODO: implement this method
    }
{{/methods}}
};
