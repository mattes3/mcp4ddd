import { describe, it } from 'vitest';

describe('{{serviceName}}', () => { 
    it('{{serviceName}}', () => { 
        // test the method {{serviceName}}({{formattedParameters}}){{#resultType}}: {{resultType}}{{/resultType}}
    });
});
