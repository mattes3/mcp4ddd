// @DomainService
export const {{serviceName}} =
    ({{formattedInjectedDependencies}}) =>
    ({{formattedParameters}}){{#if resultType}}: {{resultType}}{{/if}} => {
        //TODO: implement this service function
    };
