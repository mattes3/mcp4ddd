import { type SingleError, singleError, type TechError, techError, type ValidationError } from '../../BasicErrorTypes.js';

export type {{serviceErrorType}} = SingleError<'{{serviceErrorType}}'> | TechError | ValidationError;

export const create{{serviceErrorType}} = (yourErrorInformation: string): {{serviceErrorType}} =>
    singleError('{{serviceErrorType}}', `{{serviceName}} failed: ${yourErrorInformation}`);
